var getEvents = function(tags) {
	
	var params = {
		key: 
	};

	$.ajax({
		url: ,
		data: params
	});

	console.log(this);
}

$(function(){
	var searchTerm = $('#query').val();
	var tags = searchTerm
	getEvents(tags);
});